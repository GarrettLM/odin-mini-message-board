<html>
  <body>
    <h1><%= title %></h1>
    <ul>
      <% for (let i = 0; i < messages.length; i++) { %>
        <li>
          <div><%= messages[i].user %> </div>
          <div><%= messages[i].text %> </div>
          <a href=/message/<%= messages[i].id%>><button>Open</button></a>
        </li>
      <% } %>
    </ul>
  </body>
</html>